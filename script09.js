const instrumentos = ["guitarra", "bateria", "violao", "teclado"]; // Array de instrumentos
const instPrecos = [1200.0, 3000.0, 500.0, 515.0]; // Array de preços correspondentes aos instrumentos

// Exibir o quarto instrumento e seu preço
console.log(instrumentos[3], instPrecos[3]);

const qtds = [3, 5, 12, 3]; // Array de quantidades de cada instrumento

// Calcular e exibir o valor total para 3 unidades da guitarra
const resultado = qtds[0] * instPrecos[0];
console.log(resultado);

// Exemplos de arrays individuais para cada instrumento (não totalmente utilizados no contexto atual)
const guitarra = ["guitarra"];
const bateria = ["bateria"];
const violao = ["violao"];
const teclado = ["teclado"];
